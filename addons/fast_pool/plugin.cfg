[plugin]

name="FastPool"
description="The object pool pattern is a software creational design pattern that uses a set of initialized objects kept ready to use – a 'pool' – rather than allocating and destroying them on demand"
author="Sempitern0"
version="1.1.0"
script="plugin.gd"
